[package]
name = "day10-part1"
version = "0.1.0"
edition = "2024"

[dependencies]
regex = "1"

[dev-dependencies]
criterion = { version = "0.5", features = ["html_reports"] }

[[bench]]
name = "bench"
harness = false